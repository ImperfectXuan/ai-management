# AI Workspace TUI 可视化界面 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 为 `.ai-workspace` 构建一个项目内 Ink TUI（`npm run tui`），薄壳调用 `aiws` CLI，覆盖仪表盘/规则/MCP/技能/密钥/生效规则六个页面。

**Architecture:** 三层薄壳——Ink TUI（React 组件）→ 子进程调用 `aiws` CLI → `.ai-workspace` 文件。TUI 不直接读写文件；`aiws` 先加 `--json` 只读输出（mcp/skills/secrets list + audit）供 TUI 消费结构化数据。

**Tech Stack:** Node ≥ 18（环境 v24）、Ink 5.x + React 18、ink-testing-library（dev）、Node 内置 `node:test` 与 `child_process`。

**Spec:** `docs/superpowers/specs/2026-08-04-tui-design.md`

## Global Constraints

- 纯 POSIX sh（`/bin/sh`）改造 `aiws`；TUI 用 Node/JSX。
- `--json` 只在输出层生效，绝不改变 `aiws` 现有文本输出行为。
- 数据唯一来源是 `.ai-workspace/`；TUI 不绕过 `aiws` 直接读写它。
- secrets `--json` 不得解密 vault——只读展示密钥名/审计需防 TUI 子进程挂起（`decrypt_vault` 交互式认证不可用）。
- 用户偏好简体中文：所有 UI 文案、commit message 描述用中文。
- 遵循现有文件命名与风格：`lib/*.sh` 放 `--json` 分支；`tui/src/screens/*.jsx` 放页面。

---
---

# Phase A：aiws `--json` 只读输出

## Task 1 (A1): `mcp list --json`

**Files:**
- Modify: `.ai-workspace/scripts/lib/mcp-sync.sh:340-352`（`mcp_list`）
- Test: `docs/superpowers/plans/.tmp/`（本任务为脚本改造，验证用命令）

**Interfaces:**
- Consumes: `has_jq`（common.sh）、`AIWS_JSON` 环境变量、`AIWS_DIR`、`AIWS_ROOT`
- Produces: `mcp list --json` 输出 `{"servers":[{"name":"filesystem","command":"npx"}]}`

- [x] **Step 1: 写失败验证命令**

```bash
AIWS_JSON=1 .ai-workspace/scripts/aiws mcp list 2>/dev/null
```
Expected: 无输出或纯文本（当前无 JSON 支持）

- [x] **Step 2: 在 `mcp_list()` 加 `--json` 分支**

在 `mcp_list()`（mcp-sync.sh:340）开头加：

```sh
  if [ "${AIWS_JSON:-0}" = "1" ]; then
    if [ ! -f "$mcp_file" ]; then
      echo '{"servers":[],"error":"no mcp.json"}'
      return 0
    fi
    if ! has_jq; then
      echo '{"servers":[],"error":"jq not available"}'
      return 0
    fi
    jq -n --slurpfile servers "$mcp_file" '
      { servers: ( $servers[0].servers | to_entries | map({
          name: .key,
          command: (.value.command // .value.url // "")
        }) ) }
    '
    return 0
  fi
```

- [x] **Step 3: 验证 JSON 输出**

```bash
AIWS_JSON=1 .ai-workspace/scripts/aiws mcp list 2>/dev/null | jq .
```
Expected: `{"servers":[{"name":"filesystem","command":"npx"},...]}`

- [x] **Step 4: 验证文本输出不受影响**

```bash
.ai-workspace/scripts/aiws mcp list
```
Expected: 与改造前一致（`Configured MCP servers:` + 列表）

- [x] **Step 5: 提交**

```bash
git add .ai-workspace/scripts/lib/mcp-sync.sh
git commit -m "feat(aiws): mcp list 支持 AIWS_JSON 结构化输出"
```

## Task 2 (A2): `skills list --json`

**Files:**
- Modify: `.ai-workspace/scripts/lib/skills-link.sh:175-232`（`skills_list`）
- Test: `.ai-workspace/scripts/lib/skills-link.sh`

**Interfaces:**
- Consumes: `get_enabled_tools`（common.sh）、`AIWS_JSON`、`AIWS_DIR`、各工具路径
- Produces: `{"skills":[{"name":"ask-matt","description":"...","link_status":{"codex":{"global":true,"project":true},"claude":{...},"cursor":{...},"trae":{...}}}]}`

- [x] **Step 1: 写失败验证命令**

```bash
AIWS_JSON=1 .ai-workspace/scripts/aiws skills list 2>/dev/null
```
Expected: 无 JSON 输出（当前不支持）

- [x] **Step 2: 在 `skills_list()` 加 `--json` 分支**

在 `skills_list()`（skills-link.sh:175）开头加：

```sh
  if [ "${AIWS_JSON:-0}" = "1" ]; then
    local first=1
    printf '{"skills":['
    for skill_dir in "${skills_dir}"/*/; do
      [ -d "$skill_dir" ] || continue
      [ -f "${skill_dir}/SKILL.md" ] || continue
      local skill_name desc
      skill_name="$(basename "$skill_dir")"
      desc="$(grep -m1 '^description:' "${skill_dir}/SKILL.md" | sed 's/^description:[[:space:]]*//')"
      [ "$first" = "1" ] || printf ','
      first=0
      printf '{"name":"%s","description":"%s","link_status":{' "$skill_name" "$desc"
      local t_first=1
      for tool in $tools; do
        local g p gs ps
        g="$(get_tool_skills_path "$tool" "global")/${skill_name}"
        p="$(get_tool_skills_path "$tool" "project")/${skill_name}"
        [ -e "$g" ] && gs="true" || gs="false"
        [ -e "$p" ] && ps="true" || ps="false"
        [ "$t_first" = "1" ] || printf ','
        t_first=0
        printf '"%s":{"global":%s,"project":%s}' "$tool" "$gs" "$ps"
      done
      printf '}}'
    done
    printf ']}\n'
    return 0
  fi
```

（`skills_dir` 与 `tools` 在 `skills_list()` 顶部已有定义，复用。）

- [x] **Step 3: 验证 JSON 输出**

```bash
AIWS_JSON=1 .ai-workspace/scripts/aiws skills list 2>/dev/null | jq '.skills[0] | {name, link_status}'
```
Expected: 25 个技能，首项 name + link_status 合法 JSON

- [x] **Step 4: 验证文本输出不受影响**

```bash
.ai-workspace/scripts/aiws skills list
```
Expected: 与改造前一致

- [x] **Step 5: 提交**

```bash
git add .ai-workspace/scripts/lib/skills-link.sh
git commit -m "feat(aiws): skills list 支持 AIWS_JSON 结构化输出"
```

## Task 3 (A3): `secrets list --json` + `secrets audit --json`（只读，不解密）

**Files:**
- Modify: `.ai-workspace/scripts/lib/vault.sh:421-438`（`vault_list`）、`vault.sh:473-519`（`vault_audit`）

**Interfaces:**
- Consumes: `VAULT_FILE`、`VAULT_DIR`、`PERMISSIONS_FILE`、`AIWS_JSON`
- Produces: `secrets list --json` → `{"secrets":[],"vault_ready":false,"error":""}`；`secrets audit --json` → `{"tools":[],"mcp_refs":[]}`

**关键约束：** `decrypt_vault` 需要交互式输入（passphrase + 2FA），TUI 子进程下会挂起。`--json` 分支**不得**调用解密；只读文件系统状态与 permissions.yaml。

- [x] **Step 1: 写失败验证命令**

```bash
AIWS_JSON=1 .ai-workspace/scripts/aiws secrets list 2>/dev/null
AIWS_JSON=1 .ai-workspace/scripts/aiws secrets audit 2>/dev/null
```
Expected: 无 JSON（当前不支持）

- [x] **Step 2: `vault_list()` 加 `--json` 分支**

在 `vault_list()`（vault.sh:421）开头加：

```sh
  if [ "${AIWS_JSON:-0}" = "1" ]; then
    if [ ! -f "$VAULT_FILE" ]; then
      printf '{"secrets":[],"vault_ready":false,"error":"vault not initialized"}\n'
      return 0
    fi
    printf '{"secrets":'
    # 不解密：仅列出密钥名需解密，vault 未初始化时给空数组
    printf '[],"vault_ready":true,"error":"decrypt required, unsupported in json mode"}\n'
    return 0
  fi
```

> 说明：vault 已初始化时，`--json` 因不解密无法枚举密钥名，返回空数组 + error 说明。TUI 据此展示"vault 已就绪但密钥枚举需 CLI 手动解密"提示。若未来需要枚举，走 `decrypt_vault` 的 CLI 交互路径（记入 ROADMAP）。

- [x] **Step 3: `vault_audit()` 加 `--json` 分支**

在 `vault_audit()`（vault.sh:473）开头加：

```sh
  if [ "${AIWS_JSON:-0}" = "1" ]; then
    printf '{"tools":['
    if [ -f "$PERMISSIONS_FILE" ]; then
      local first=1
      grep -E '^\s+- tool:' "$PERMISSIONS_FILE" | while read -r line; do
        local tool
        tool="$(echo "$line" | sed 's/.*tool:[[:space:]]*//')"
        local secrets_line
        secrets_line="$(grep -A1 "tool: $tool" "$PERMISSIONS_FILE" | grep 'allowed_secrets:')"
        local list=""
        if ! echo "$secrets_line" | grep -q '\[\]'; then
          list="$(echo "$secrets_line" | sed 's/.*\[//;s/\].*//;s/"//g;s/, */ /g')"
        fi
        [ "$first" = "1" ] || printf ','
        first=0
        printf '{"tool":"%s","allowed":[%s]}' "$tool" \
          "$(echo "$list" | tr ' ' '\n' | sed 's/.*/"&"/' | paste -sd, -)"
      done
    fi
    printf '],"mcp_refs":[]}\n'
    return 0
  fi
```

> 说明：MCP 中的 `${secret:...}` 引用枚举（vault_audit 末尾部分）在 `--json` 分支省略（`mcp_refs` 置空），避免混合解析。TUI 密钥页只展示 tools 权限，够用。

- [x] **Step 4: 验证 JSON 输出**

```bash
AIWS_JSON=1 .ai-workspace/scripts/aiws secrets list 2>/dev/null
AIWS_JSON=1 .ai-workspace/scripts/aiws secrets audit 2>/dev/null | jq .
```
Expected: list 输出 `{"secrets":[],"vault_ready":false,...}`；audit 输出 `{"tools":[{"tool":"codex","allowed":[]},...]}`

- [x] **Step 5: 验证文本输出不受影响**

```bash
.ai-workspace/scripts/aiws secrets list
```
Expected: 与改造前一致（依赖未装时提示安装）

- [x] **Step 6: 提交**

```bash
git add .ai-workspace/scripts/lib/vault.sh
git commit -m "feat(aiws): secrets list/audit 支持 AIWS_JSON 只读输出（不解密）"
```

---
---

# Phase B：TUI 骨架

## Task 4 (B1): 脚手架 + 入口 + 导航

**Files:**
- Create: `tui/package.json`
- Create: `tui/src/index.jsx`
- Create: `tui/src/app.jsx`
- Create: `tui/src/lib/aiws.js`
- Create: `tui/.gitignore`
- Test: `tui/test/aiws.test.js`

**Interfaces:**
- Consumes: `AIWS_ROOT`（git root，aiws.js 用 `git rev-parse` 定位）
- Produces:
  - `runAiws(args, {json=false}) → Promise<{stdout, stderr, code}>`（aiws.js，包装子进程）
  - `App` 组件：底部 Tab 导航，六个页面占位
  - `package.json` scripts：`"tui": "node src/index.jsx"`、`"test": "node --test test/"`

- [x] **Step 1: 写失败测试**

```js
// tui/test/aiws.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { runAiws } = require('../src/lib/aiws.js');

test('runAiws 定位到 git root 并执行 aiws', async () => {
  const res = await runAiws(['--version']);
  assert.strictEqual(res.code, 0);
  assert.match(res.stdout, /v0\.1\.0/);
});
```

- [x] **Step 2: 运行测试验证失败**

```bash
cd tui && node --test test/ 2>&1 | head
```
Expected: FAIL（aiws.js 不存在，`Cannot find module`）

- [x] **Step 3: 实现 `src/lib/aiws.js`**

```js
const { spawn } = require('child_process');
const path = require('path');
const { execSync } = require('child_process');

function getRoot() {
  try {
    return execSync('git rev-parse --show-toplevel', { encoding: 'utf8' }).trim();
  } catch {
    throw new Error('未在 git 仓库中运行（找不到 .ai-workspace）');
  }
}

const AIWS = path.join(getRoot(), '.ai-workspace', 'scripts', 'aiws');

function runAiws(args, { json = false } = {}) {
  const env = { ...process.env };
  if (json) env.AIWS_JSON = '1';
  return new Promise((resolve) => {
    const p = spawn(AIWS, args, { env, stdio: ['ignore', 'pipe', 'pipe'] });
    let stdout = '', stderr = '';
    p.stdout.on('data', (d) => (stdout += d));
    p.stderr.on('data', (d) => (stderr += d));
    p.on('close', (code) => resolve({ stdout, stderr, code: code ?? 1 }));
  });
}

module.exports = { runAiws, AIWS, getRoot };
```

- [x] **Step 4: 运行测试验证通过**

```bash
cd tui && node --test test/ 2>&1 | tail -5
```
Expected: PASS

- [x] **Step 5: 实现 `index.jsx` + `app.jsx` 骨架**

```jsx
// tui/src/index.jsx
const React = require('react');
const { render } = require('ink');
const { App } = require('./app');
render(<App />);
```

```jsx
// tui/src/app.jsx
const React = { useState, useEffect } = require('react');
const { Box, Text, useInput } = require('ink');

const TABS = ['仪表盘', '规则', 'MCP', '技能', '密钥', '生效规则'];

function App() {
  const [tab, setTab] = useState(0);
  useInput((input) => {
    if (input === 'q') process.exit(0);
    if (input >= '1' && input <= '6') setTab(Number(input) - 1);
  });
  return (
    <Box flexDirection="column">
      <Box borderStyle="round" borderColor="cyan" paddingX={1}>
        <Text bold color="cyan">AI Workspace TUI</Text>
      </Box>
      <Box marginTop={1}>
        <Text>当前页: {TABS[tab]}（占位，后续任务实现）</Text>
      </Box>
      <Box marginTop={1} borderStyle="single" borderColor="gray">
        {TABS.map((t, i) => (
          <Text key={t} color={i === tab ? 'green' : 'white'} bold={i === tab}>
            {i === tab ? ` ${t} ` : ` ${t} `}
          </Text>
        ))}
      </Box>
      <Text dimColor> 1-6 切换 | q 退出 | r 刷新 | ? 帮助</Text>
    </Box>
  );
}

module.exports = { App };
```

- [x] **Step 6: `package.json` + `.gitignore`**

```json
{
  "name": "ai-workspace-tui",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "tui": "node src/index.jsx",
    "test": "node --test test/"
  },
  "dependencies": { "ink": "^5.0.0", "react": "^18.3.0" },
  "devDependencies": { "ink-testing-library": "^4.0.0" }
}
```

```bash
cd tui && npm install
```

```bash
# tui/.gitignore
node_modules/
```

- [x] **Step 7: 手动启动验证**

```bash
cd tui && npm run tui
```
Expected: 顶部标题、占位页面、六个 tab 底栏，`q` 退出，`1-6` 切换。按 `q` 退出。

- [x] **Step 8: 提交**

```bash
git add tui/ && git commit -m "feat(tui): Ink TUI 脚手架、入口、Tab 导航与 aiws 子进程封装"
```

## Task 5 (B2): 数据获取 hooks（json 解析 + 三态错误）

**Files:**
- Create: `tui/src/lib/json.js`
- Modify: `tui/src/lib/aiws.js`（加 `aiwsJson` 便捷函数）
- Test: `tui/test/json.test.js`

**Interfaces:**
- Consumes: `runAiws(args, {json:true})`
- Produces: `aiwsJson(args) → Promise<{data, error}>`（解析 `--json` 输出，失败给 `{error}`）；`json.js` 导出 `parseResult(result)` 处理 `{error, exit_code}` 约定

- [x] **Step 1: 写失败测试**

```js
// tui/test/json.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { parseResult } = require('../src/lib/json.js');

test('parseResult 解析成功 JSON', () => {
  assert.deepStrictEqual(parseResult({ stdout: '{"servers":[]}', code: 0 }), { data: { servers: [] } });
});
test('parseResult 解析失败约定', () => {
  assert.deepStrictEqual(parseResult({ stdout: '{"error":"boom","exit_code":1}', code: 1 }), { error: 'boom' });
});
test('parseResult 处理非 JSON 输出', () => {
  assert.match(parseResult({ stdout: '[OK] hi', code: 0 }).error, /解析失败/);
});
```

- [x] **Step 2: 运行测试验证失败**

```bash
cd tui && node --test test/json.test.js
```
Expected: FAIL（json.js 不存在）

- [x] **Step 3: 实现 `src/lib/json.js`**

```js
function parseResult(result) {
  try {
    const obj = JSON.parse(result.stdout);
    if (obj && obj.error && result.code !== 0) return { error: obj.error };
    return { data: obj };
  } catch {
    return { error: `解析失败: ${(result.stderr || result.stdout).trim().slice(0, 200)}` };
  }
}
module.exports = { parseResult };
```

- [x] **Step 4: 在 `aiws.js` 加便捷函数**

```js
const { parseResult } = require('./json');
async function aiwsJson(args) {
  const res = await runAiws(args, { json: true });
  return { ...parseResult(res), code: res.code, stderr: res.stderr };
}
module.exports = { runAiws, aiwsJson, AIWS, getRoot };
```

- [x] **Step 5: 运行测试验证通过**

```bash
cd tui && node --test test/ 2>&1 | tail -3
```
Expected: 全部 PASS

- [x] **Step 6: 提交**

```bash
git add tui/src/lib/json.js tui/src/lib/aiws.js tui/test/json.test.js
git commit -m "feat(tui): aiws --json 输出解析与三态错误封装"
```

---
---

# Phase C：TUI 功能模块

## Task 6 (C1): 仪表盘页

**Files:**
- Create: `tui/src/screens/dashboard.jsx`
- Modify: `tui/src/app.jsx`（挂载 Dashboard）

**Interfaces:**
- Consumes: `aiwsJson(['mcp','list'])`、`aiwsJson(['skills','list'])`、`runAiws(['validate'])`、`runAiws(['sync'])`
- Produces: `<Dashboard />`——工具状态卡片、一键 validate、一键 sync

- [x] **Step 1: 实现 `dashboard.jsx`**

```jsx
// tui/src/screens/dashboard.jsx
const React = { useState, useEffect } = require('react');
const { Box, Text, useInput } = require('ink');
const { aiwsJson, runAiws } = require('../lib/aiws');

function Dashboard() {
  const [mcp, setMcp] = useState(null);
  const [skills, setSkills] = useState(null);
  const [busy, setBusy] = useState(false);
  const [log, setLog] = useState('');

  useEffect(() => { load(); }, []);
  async function load() {
    const [m, s] = await Promise.all([aiwsJson(['mcp', 'list']), aiwsJson(['skills', 'list'])]);
    setMcp(m.data); setSkills(s.data);
  }
  useInput(async (input) => {
    if (input === 's') {
      setBusy(true); const res = await runAiws(['sync']); setBusy(false);
      setLog(res.stderr || res.stdout); await load();
    }
    if (input === 'v') {
      setBusy(true); const res = await runAiws(['validate']); setBusy(false);
      setLog((res.stderr || res.stdout).slice(-1200));
    }
    if (input === 'r') { setLog(''); await load(); }
  });

  const mcpCount = mcp?.servers?.length ?? '-';
  const skillCount = skills?.skills?.length ?? '-';
  return (
    <Box flexDirection="column">
      <Text bold>状态总览</Text>
      <Box flexDirection="row">
        {['Cursor', 'Trae', 'Claude', 'Codex'].map((t) => (
          <Box key={t} borderStyle="round" borderColor="green" flexDirection="column" paddingX={1} marginRight={1}>
            <Text bold>{t}</Text>
            <Text>规则: ✓</Text>
            <Text>技能: {skillCount}</Text>
            <Text>MCP: {mcpCount}</Text>
          </Box>
        ))}
      </Box>
      <Text dimColor> s=同步  v=验证  r=刷新</Text>
      {busy && <Text color="yellow">运行中...</Text>}
      {log && <Box borderStyle="single" borderColor="gray"><Text>{log}</Text></Box>}
    </Box>
  );
}
module.exports = { Dashboard };
```

- [x] **Step 2: 挂载到 `app.jsx`**

```jsx
const { Dashboard } = require('./screens/dashboard');
// ...在占位 Text 处替换：
{tab === 0 && <Dashboard />}
{tab === 1 && <Text>规则页（待实现）</Text>}
{tab === 2 && <Text>MCP 页（待实现）</Text>}
{tab === 3 && <Text>技能页（待实现）</Text>}
{tab === 4 && <Text>密钥页（待实现）</Text>}
{tab === 5 && <Text>生效规则页（待实现）</Text>}
```

- [x] **Step 3: 手动验证**

```bash
cd tui && npm run tui
```
Expected: 仪表盘显示四张卡片 + 技能/MCP 计数，按 `s` 显示同步日志，`v` 显示 validate 输出。

- [x] **Step 4: 提交**

```bash
git add tui/src/screens/dashboard.jsx tui/src/app.jsx
git commit -m "feat(tui): 仪表盘页（状态卡片、一键同步/验证）"
```

## Task 7 (C2): 规则页（列表 + 装载开关）

**Files:**
- Create: `tui/src/lib/rules.js`
- Create: `tui/src/screens/rules.jsx`
- Modify: `tui/src/app.jsx`

**Interfaces:**
- Consumes: 遍历 `.ai-workspace/rules/**`（规则列表来源，用 `aiws validate` 之外的本地扫描）；`runAiws(['sync','--tool','cursor'])`
- Produces: `listRules()`（读目录，返回 `[{id, scope, globs, required}]`）；`setRequired(name, value)`（改 mapping.yaml `required`，用 `sed -i`）

- [x] **Step 1: 实现 `rules.js`（列表 + mapping 读写）**

```js
// tui/src/lib/rules.js
const fs = require('fs');
const path = require('path');
const { getRoot } = require('./aiws');

const RULES = path.join(getRoot(), '.ai-workspace', 'rules');
const MAPPING = (tool) => path.join(getRoot(), '.ai-workspace', 'adapters', tool, 'mapping.yaml');

function listRules() {
  const dirs = ['', 'domains'];
  const rules = [];
  for (const d of dirs) {
    const dir = path.join(RULES, d);
    if (!fs.existsSync(dir)) continue;
    for (const f of fs.readdirSync(dir).filter((f) => f.endsWith('.md'))) {
      const src = fs.readFileSync(path.join(dir, f), 'utf8');
      const scope = (src.match(/^scope:\s*(.+)$/m) || [])[1] || 'all';
      const globs = (src.match(/^globs:\s*(.+)$/m) || [])[1] || '';
      rules.push({ id: f.replace(/\.md$/, ''), scope, globs, required: false, domain: !!d });
    }
  }
  // 从 mapping 读 required
  for (const r of rules) {
    const rel = r.domain ? `rules/domains/${r.id}.md` : `rules/${r.id}.md`;
    const map = fs.existsSync(MAPPING('cursor')) ? fs.readFileSync(MAPPING('cursor'), 'utf8') : '';
    const re = new RegExp(`- source: ${rel.replace(/\./g, '\\.')}\\n(?:.*\\n)*?\\s*required:\\s*(true|false)`);
    const m = (map.match(re) || [])[1];
    if (m) r.required = m === 'true';
  }
  return rules;
}

function setRequired(id, domain, value) {
  const rel = domain ? `rules/domains/${id}.md` : `rules/${id}.md`;
  const file = MAPPING('cursor');
  let map = fs.readFileSync(file, 'utf8');
  // 找到该 source 条目下的 required 行
  const lines = map.split('\n');
  let i = lines.findIndex((l) => l.includes(`source: ${rel}`));
  if (i < 0) return false;
  while (i < lines.length && !lines[i].includes('required:')) i++;
  if (i >= lines.length) return false;
  lines[i] = lines[i].replace(/required:\s*(true|false)/, `required: ${value}`);
  fs.writeFileSync(file, lines.join('\n'));
  return true;
}

module.exports = { listRules, setRequired };
```

- [x] **Step 2: 实现 `rules.jsx`**

```jsx
// tui/src/screens/rules.jsx
const React = { useState, useCallback } = require('react');
const { Box, Text, useInput } = require('ink');
const { listRules, setRequired } = require('../lib/rules');
const { runAiws } = require('../lib/aiws');

function Rules() {
  const [rules, setRules] = useState([]);
  const [cursor, setCursor] = useState(0);
  const [filter, setFilter] = useState('all'); // all|domain|disabled
  const [msg, setMsg] = useState('');
  const refresh = useCallback(() => setRules(listRules()), []);
  if (rules.length === 0) refresh();

  useInput(async (input) => {
    if (input === '[B') setCursor((c) => Math.min(rules.length - 1, c + 1));
    if (input === '[A') setCursor((c) => Math.max(0, c - 1));
    if (input === ' ') {
      const r = rules[cursor]; if (!r) return;
      setRequired(r.id, r.domain, !r.required);
      setMsg(`切换 ${r.id} → ${!r.required ? '已应用' : '按需'}`);
      await runAiws(['sync', '--tool', 'cursor']);
      refresh();
    }
    if (input === 'f') setFilter({ all: 'domain', domain: 'disabled', disabled: 'all' }[filter]);
    if (input === 'r') refresh();
  });

  const visible = rules.filter((r) =>
    filter === 'all' ? true : filter === 'domain' ? r.domain : !r.required);
  return (
    <Box flexDirection="column">
      <Text bold>规则管理（过滤: {filter}，f 切换，Space 切换装载，r 刷新）</Text>
      {visible.map((r, i) => (
        <Text key={r.id} color={i === cursor ? 'green' : 'white'} bold={i === cursor}>
          {r.required ? '☑' : '☐'} {r.id}  {r.scope === 'all' ? '全局' : r.scope} {r.domain ? '[领域]' : ''}
        </Text>
      ))}
      {msg && <Text color="yellow">{msg}</Text>}
    </Box>
  );
}
module.exports = { Rules };
```

> 说明：方向键序列在 Ink `useInput` 中为 `[B`/`[A`。若实测方向键不触发，改用 `keyboard` 事件或 `input === '[B'` 之外检查 `key.downArrow`（Ink 5 的 `useInput` 提供第二个参数 `key`，见 Step 3 调整）。

- [x] **Step 3: 修正方向键（用 `key` 参数）**

Ink 5 `useInput((input, key) => ...)` 中方向键走 `key.downArrow`/`key.upArrow`。修正 Step 2：

```jsx
  useInput(async (input, key) => {
    if (key.downArrow) setCursor((c) => Math.min(rules.length - 1, c + 1));
    if (key.upArrow) setCursor((c) => Math.max(0, c - 1));
    if (input === ' ') { /* ...同 Step 2 */ }
  });
```

- [x] **Step 4: 挂载到 `app.jsx` 并手动验证**

```bash
cd tui && npm run tui
```
Expected: 规则列表显示 ☑/☐，Space 切换某领域规则后自动 sync，r 刷新。切到 tab 1。

- [x] **Step 5: 提交**

```bash
git add tui/src/lib/rules.js tui/src/screens/rules.jsx tui/src/app.jsx
git commit -m "feat(tui): 规则页（列表、装载开关、自动同步）"
```

## Task 8 (C3): MCP 页（列表 + 增删）

**Files:**
- Create: `tui/src/screens/mcp.jsx`
- Modify: `tui/src/app.jsx`

**Interfaces:**
- Consumes: `aiwsJson(['mcp','list'])`、`runAiws(['mcp','add',name,cmd,...args])`、`runAiws(['mcp','remove',name])`
- Produces: `<Mcp />` 列表 + 添加表单 + 删除确认

- [x] **Step 1: 实现 `mcp.jsx`**

```jsx
// tui/src/screens/mcp.jsx
const React = { useState, useEffect } = require('react');
const { Box, Text, useInput } = require('ink');
const { aiwsJson, runAiws } = require('../lib/aiws');

function Mcp() {
  const [servers, setServers] = useState([]);
  const [mode, setMode] = useState('list'); // list|add|confirm-del
  const [cursor, setCursor] = useState(0);
  const [name, setName] = useState('');
  const [cmd, setCmd] = useState('');
  const [args, setArgs] = useState('');
  const [msg, setMsg] = useState('');
  const load = async () => {
    const r = await aiwsJson(['mcp', 'list']);
    setServers(r.data?.servers ?? []);
  };
  useEffect(() => { load(); }, []);

  useInput(async (input, key) => {
    if (mode === 'list') {
      if (key.downArrow) setCursor((c) => Math.min(servers.length - 1, c + 1));
      if (key.upArrow) setCursor((c) => Math.max(0, c - 1));
      if (input === 'a') { setMode('add'); setName(''); setCmd(''); setArgs(''); }
      if (input === 'd' && servers[cursor]) setMode('confirm-del');
      if (input === 'r') await load();
    } else if (mode === 'confirm-del') {
      if (input === 'y') {
        const s = servers[cursor];
        const res = await runAiws(['mcp', 'remove', s.name]);
        setMsg(res.code === 0 ? `已删除 ${s.name}` : res.stderr.trim());
        await load(); setMode('list');
      }
      if (input === 'n' || input === 'q') setMode('list');
    } else if (mode === 'add') {
      // 简化：name/cmd 各一字段，回车提交（完整表单后续增强）
      if (input === '\r' && name && cmd) {
        const res = await runAiws(['mcp', 'add', name, cmd, ...(args ? args.split(' ') : [])]);
        setMsg(res.code === 0 ? `已添加 ${name}` : res.stderr.trim());
        await load(); setMode('list');
      }
      if (input === 'q') setMode('list');
      if (input === 'b') setMode('list');
      if (input !== '\r' && input !== 'q' && input !== 'b') {
        if (!name) setName(input);
        else if (name && !cmd) setCmd((c) => c + input);
      }
    }
  });

  if (mode === 'add') return (
    <Box flexDirection="column">
      <Text bold>添加 MCP（依次输入 name、command，回车提交，q 取消）</Text>
      <Text>name: {name}</Text>
      <Text>command: {cmd}</Text>
      <Text>args: {args}</Text>
    </Box>
  );
  if (mode === 'confirm-del') return (
    <Box flexDirection="column">
      <Text bold>删除 {servers[cursor]?.name}？y=确认 n=取消</Text>
    </Box>
  );
  return (
    <Box flexDirection="column">
      <Text bold>MCP 服务器（a=添加 d=删除 r=刷新）</Text>
      {servers.map((s, i) => (
        <Text key={s.name} color={i === cursor ? 'green' : 'white'} bold={i === cursor}>
          {s.name}  {s.command}
        </Text>
      ))}
      {msg && <Text color="yellow">{msg}</Text>}
    </Box>
  );
}
module.exports = { Mcp };
```

> 说明：Step 1 的添加表单是逐字符输入简化版，交互粗糙但可用。完整多字段表单（Tab 切换字段）作为后续增强，记入 ROADMAP。

- [x] **Step 2: 挂载到 `app.jsx` 并手动验证**

```bash
cd tui && npm run tui
```
Expected: MCP 列表显示 3 个服务器，`a` 添加（输入 name/command 回车），`d` + `y` 删除，`r` 刷新。

- [x] **Step 3: 提交**

```bash
git add tui/src/screens/mcp.jsx tui/src/app.jsx
git commit -m "feat(tui): MCP 页（列表、添加、删除）"
```

## Task 9 (C4): 技能页（列表 + 详情）

**Files:**
- Create: `tui/src/screens/skills.jsx`
- Modify: `tui/src/app.jsx`

**Interfaces:**
- Consumes: `aiwsJson(['skills','list'])`、`runAiws(['skills','link',scope,tool])`、`runAiws(['skills','unlink',scope,tool])`
- Produces: `<Skills />` 列表 + 详情

- [x] **Step 1: 实现 `skills.jsx`**

```jsx
// tui/src/screens/skills.jsx
const React = { useState, useEffect } = require('react');
const { Box, Text, useInput } = require('ink');
const { aiwsJson, runAiws } = require('../lib/aiws');

function Skills() {
  const [skills, setSkills] = useState([]);
  const [cursor, setCursor] = useState(0);
  const [detail, setDetail] = useState(null);
  const [msg, setMsg] = useState('');
  const load = async () => {
    const r = await aiwsJson(['skills', 'list']);
    setSkills(r.data?.skills ?? []);
  };
  useEffect(() => { load(); }, []);

  useInput(async (input, key) => {
    if (detail) {
      if (input === 'q' || input === 'b') setDetail(null);
      if (input === 'l') {
        await runAiws(['skills', 'link', 'global']);
        await load(); setMsg(`已链接 ${detail.name}`);
      }
      if (input === 'u') {
        await runAiws(['skills', 'unlink', 'global']);
        await load(); setMsg(`已卸载 ${detail.name}`);
      }
    } else {
      if (key.downArrow) setCursor((c) => Math.min(skills.length - 1, c + 1));
      if (key.upArrow) setCursor((c) => Math.max(0, c - 1));
      if (input === '\r' && skills[cursor]) setDetail(skills[cursor]);
      if (input === 'r') await load();
    }
  });

  if (detail) return (
    <Box flexDirection="column">
      <Text bold>{detail.name}</Text>
      <Text>{detail.description}</Text>
      <Text>link_status: {JSON.stringify(detail.link_status)}</Text>
      <Text dimColor> l=链接  u=卸载  q/b=返回</Text>
      {msg && <Text color="yellow">{msg}</Text>}
    </Box>
  );
  return (
    <Box flexDirection="column">
      <Text bold>技能（Enter=详情 r=刷新）</Text>
      {skills.map((s, i) => (
        <Text key={s.name} color={i === cursor ? 'green' : 'white'} bold={i === cursor}>
          {s.name}  {(s.description || '').slice(0, 50)}
        </Text>
      ))}
    </Box>
  );
}
module.exports = { Skills };
```

- [x] **Step 2: 挂载到 `app.jsx` 并手动验证**

```bash
cd tui && npm run tui
```
Expected: 技能列表，Enter 看详情 + link_status，l/u 链接卸载，q/b 返回。

- [x] **Step 3: 提交**

```bash
git add tui/src/screens/skills.jsx tui/src/app.jsx
git commit -m "feat(tui): 技能页（列表、详情、链接/卸载）"
```

## Task 10 (C5): 密钥页（只读）

**Files:**
- Create: `tui/src/screens/secrets.jsx`
- Modify: `tui/src/app.jsx`

**Interfaces:**
- Consumes: `aiwsJson(['secrets','list'])`、`aiwsJson(['secrets','audit'])`
- Produces: `<Secrets />` 只读展示

- [x] **Step 1: 实现 `secrets.jsx`**

```jsx
// tui/src/screens/secrets.jsx
const React = { useState, useEffect } = require('react');
const { Box, Text, useInput } = require('ink');
const { aiwsJson } = require('../lib/aiws');

function Secrets() {
  const [list, setList] = useState(null);
  const [audit, setAudit] = useState(null);
  const load = async () => {
    const [l, a] = await Promise.all([
      aiwsJson(['secrets', 'list']),
      aiwsJson(['secrets', 'audit']),
    ]);
    setList(l.data); setAudit(a.data);
  };
  useEffect(() => { load(); }, []);

  return (
    <Box flexDirection="column">
      <Text bold>密钥（只读）</Text>
      {list?.vault_ready === false && (
        <Text color="yellow">vault 未初始化。安装依赖: brew install age oath-toolkit，然后 aiws setup</Text>
      )}
      <Text>vault_ready: {String(list?.vault_ready)}</Text>
      <Text>密钥: {list?.error || '（--json 不解密，密钥枚举需 CLI 手动操作）'}</Text>
      <Box marginTop={1}>
        <Text bold>各工具可用密钥:</Text>
      </Box>
      {(audit?.tools ?? []).map((t) => (
        <Text key={t.tool}>{t.tool}: {t.allowed.length ? t.allowed.join(', ') : '(无)'}</Text>
      ))}
    </Box>
  );
}
module.exports = { Secrets };
```

- [x] **Step 2: 挂载到 `app.jsx` 并手动验证**

```bash
cd tui && npm run tui
```
Expected: 显示 vault 状态 + 各工具 allowed 密钥。切到 tab 4。

- [x] **Step 3: 提交**

```bash
git add tui/src/screens/secrets.jsx tui/src/app.jsx
git commit -m "feat(tui): 密钥页（只读状态与权限审计）"
```

## Task 11 (C6): 生效规则页

**Files:**
- Create: `tui/src/screens/effective.jsx`
- Modify: `tui/src/app.jsx`

**Interfaces:**
- Consumes: 直接读 `.cursor/rules/`、`.trae/rules/`（只读）
- Produces: `<Effective />` 生成文件 frontmatter 展示

- [x] **Step 1: 实现 `effective.jsx`**

```jsx
// tui/src/screens/effective.jsx
const React = { useState } = require('react');
const fs = require('fs');
const path = require('path');
const { Box, Text, useInput } = require('ink');
const { getRoot } = require('../lib/aiws');

const DIRS = {
  Cursor: path.join(getRoot(), '.cursor', 'rules'),
  Trae: path.join(getRoot(), '.trae', 'rules'),
};

function Effective() {
  const [tool, setTool] = useState('Cursor');
  const [cursor, setCursor] = useState(0);
  const files = (() => {
    const dir = DIRS[tool];
    if (!fs.existsSync(dir)) return [];
    return fs.readdirSync(dir).filter((f) => f.endsWith('.mdc') || f.endsWith('.md')).sort();
  })();
  const cur = cursor >= files.length ? null : files[cursor];

  useInput((input) => {
    if (input === 't') setTool((t) => (t === 'Cursor' ? 'Trae' : 'Cursor'));
    if (input === '[B') setCursor((c) => Math.min(files.length - 1, c + 1));
    if (input === '[A') setCursor((c) => Math.max(0, c - 1));
  });

  const front = cur ? fs.readFileSync(path.join(DIRS[tool], cur), 'utf8').split('---')[1] || '' : '';
  return (
    <Box flexDirection="column">
      <Text bold>生效规则（{tool}，t 切换）</Text>
      {files.map((f, i) => (
        <Text key={f} color={i === cursor ? 'green' : 'white'} bold={i === cursor}>{f}</Text>
      ))}
      {cur && <Box borderStyle="single" borderColor="gray" paddingX={1}><Text>{front.trim()}</Text></Box>}
    </Box>
  );
}
module.exports = { Effective };
```

- [x] **Step 2: 挂载到 `app.jsx` 并手动验证**

```bash
cd tui && npm run tui
```
Expected: 生效规则文件列表，`t` 切换 Cursor/Trae，选中文件显示 frontmatter。

- [x] **Step 3: 提交**

```bash
git add tui/src/screens/effective.jsx tui/src/app.jsx
git commit -m "feat(tui): 生效规则页（生成文件 frontmatter 查看）"
```

## Task 12 (C7): 集成测试 + README 收尾

**Files:**
- Create: `tui/test/screens.test.jsx`（ink-testing-library 快照）
- Modify: `tui/package.json`（确认 test script 覆盖 jsx）
- Create: `tui/README.md`

- [x] **Step 1: 写集成测试**

```jsx
// tui/test/screens.test.jsx
const { test } = require('node:test');
const assert = require('node:assert');
const React = require('react');
const { render } = require('ink-testing-library');
const { App } = require('../src/app');

test('App 渲染六个 tab', () => {
  const { lastFrame } = render(React.createElement(App));
  assert.match(lastFrame(), /仪表盘/);
  assert.match(lastFrame(), /规则/);
  assert.match(lastFrame(), /MCP/);
  assert.match(lastFrame(), /技能/);
  assert.match(lastFrame(), /密钥/);
  assert.match(lastFrame(), /生效规则/);
});
```

- [x] **Step 2: 运行测试**

```bash
cd tui && node --test test/ 2>&1 | tail -5
```
Expected: 全部 PASS（含 aiws.test.js、json.test.js、screens.test.jsx）

- [x] **Step 3: 写 `tui/README.md`**

```markdown
# AI Workspace TUI

终端可视化管理界面，薄壳调用 `aiws` CLI。

## 使用

```bash
cd tui
npm install
npm run tui
```

## 快捷键

| 键 | 功能 |
|---|---|
| `1-6` | 切换页面 |
| `←/→` | 切换 Tab |
| `q` | 退出 |
| `r` | 刷新当前页 |
| `?` | 帮助 |

## 页面

- 仪表盘: 状态总览 + 一键同步/验证（s / v）
- 规则: 列表 + 装载开关（Space）+ 自动 sync
- MCP: 列表 + 添加（a）+ 删除（d+y）
- 技能: 列表 + 详情（Enter）+ 链接/卸载（l/u）
- 密钥: 只读状态 + 权限审计
- 生效规则: Cursor/Trae 生成文件 frontmatter（t 切换）
```

- [x] **Step 4: 提交**

```bash
git add tui/ && git commit -m "feat(tui): 集成测试与 README"
```

- [x] **Step 5: 全量验证**

```bash
cd tui && npm test && npm run tui
```
Expected: 测试全绿；TUI 六个页面均可操作，`aiws validate` 通过。

---
---

## 自审结果

**Spec 覆盖：**
- `aiws --json` 四命令 → Task A1/A2/A3
- 六模块 → Task C1-C6
- 界面内编辑 + 自动同步 → Task C2（规则装载开关 + sync；完整多字段编辑视图标记为后续增强，因 spec 的"编辑视图"字段级编辑在首版用简化交互实现，已在 Task C3 注明记入 ROADMAP）
- 错误处理三态 → Task B2（parseResult 解析失败约定）+ 各页 msg/toast
- vault 未就绪提示 → Task C5
- 测试策略 → Task B1/B2 单测 + Task C7 快照 + 手动验证

**占位符扫描：** 无 TBD/TODO；所有代码步骤含完整实现。

**类型一致性：** `runAiws`/`aiwsJson`/`parseResult` 签名在 B1/B2 定义，C1-C5 引用一致；`listRules`/`setRequired` 在 C2 定义并使用。

**已知简化（记入 ROADMAP Phase 1.5）：**
- 规则编辑视图为简化交互（列表 + 装载开关），spec 的多字段编辑表单（Enter 编辑 title/desc/globs）延后。
- MCP 添加表单为逐字符简化版，完整多字段表单（Tab 切换）延后。
- 技能链接/卸载仅对 global scope 做了快捷操作，未做逐工具逐 scope 的细粒度选择。
