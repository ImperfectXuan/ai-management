// electron/src/renderer/pages/settings.tsx —— 占位，后续任务实现
import React from 'react';

export function SettingsPage({ repoId }: { repoId: string }) {
  return <div className="muted">settings 页待实现（repoId: {repoId}）</div>;
}
