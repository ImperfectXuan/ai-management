// electron/src/renderer/pages/secrets.tsx —— 占位，后续任务实现
import React from 'react';

export function SecretsPage({ repoId }: { repoId: string }) {
  return <div className="muted">secrets 页待实现（repoId: {repoId}）</div>;
}
