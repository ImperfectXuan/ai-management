// electron/src/renderer/pages/skills.tsx —— 占位，后续任务实现
import React from 'react';

export function SkillsPage({ repoId }: { repoId: string }) {
  return <div className="muted">skills 页待实现（repoId: {repoId}）</div>;
}
