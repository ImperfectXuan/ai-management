// electron/src/renderer/pages/rules.tsx —— 占位，后续任务实现
import React from 'react';

export function RulesPage({ repoId }: { repoId: string }) {
  return <div className="muted">rules 页待实现（repoId: {repoId}）</div>;
}
