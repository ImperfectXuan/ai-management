// electron/src/renderer/pages/dashboard.tsx —— 占位，后续任务实现
import React from 'react';

export function DashboardPage({ repoId }: { repoId: string }) {
  return <div className="muted">dashboard 页待实现（repoId: {repoId}）</div>;
}
