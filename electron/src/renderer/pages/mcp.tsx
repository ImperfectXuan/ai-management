// electron/src/renderer/pages/mcp.tsx —— 占位，后续任务实现
import React from 'react';

export function McpPage({ repoId }: { repoId: string }) {
  return <div className="muted">mcp 页待实现（repoId: {repoId}）</div>;
}
