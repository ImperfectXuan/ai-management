// electron/src/renderer/pages/diff.tsx —— 占位，后续任务实现
import React from 'react';

export function DiffPage({ repoId }: { repoId: string }) {
  return <div className="muted">diff 页待实现（repoId: {repoId}）</div>;
}
